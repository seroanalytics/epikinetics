% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{prepare_epikinetics_data}
\alias{prepare_epikinetics_data}
\title{Validate and prepare data for epikinetics}
\usage{
prepare_epikinetics_data(
  data,
  formula = ~1,
  covariate_parameters = "all",
  participant_parameters = c("baseline", "boost_rate", "early_waning_rate",
    "late_waning_rate"),
  id = "pid",
  time = "day",
  exposure = "last_exp_day",
  biomarker = "titre_type",
  value = "value",
  biomarker_order = NULL,
  scale = c("natural", "log2"),
  reference_value = 1,
  lower_limit = NULL,
  upper_limit = NULL,
  censoring = NULL,
  priors = epikinetics_priors()
)
}
\arguments{
\item{data}{A \code{data.frame} or object inheriting from it.}

\item{formula}{One-sided R formula for participant-level covariates. Numeric
variables remain on their supplied scale; factors use the active R
contrasts. Use \code{~ 1} for no covariates.}

\item{covariate_parameters}{Kinetic parameters modified by the formula.
The default, \code{"all"}, applies the same design matrix to all six parameters,
matching the original model. Supply any subset of \code{baseline},
\code{time_to_peak}, \code{waning_duration}, \code{boost_rate}, \code{early_waning_rate}, and
\code{late_waning_rate} for a more explicit regression specification.}

\item{participant_parameters}{Kinetic parameters with participant-level
random effects. The default allows participants to differ in \code{baseline},
\code{boost_rate}, \code{early_waning_rate}, and \code{late_waning_rate}, while sharing
\code{time_to_peak} and \code{waning_duration} after conditioning on any selected
covariate effects. In other words, those timing parameters have no residual
participant random effect by default. This is a modelling assumption suited
to the package's motivating data, not a universal biological constraint.
Supply any parameter subset, or \code{"all"}, for another justified hierarchy.}

\item{id, time, exposure, biomarker, value}{Column names identifying participant,
observation time, focal exposure time, biomarker type, and measurement.}

\item{biomarker_order}{Optional complete ordering of observed biomarker
labels. If omitted, existing factor levels are preserved; otherwise the
order of first appearance in \code{data} is used. The order is stored in the
prepared object and propagated to posterior output, predictions, and plots.}

\item{scale}{Either \code{"natural"} (positive values transformed with base-2
logarithms) or \code{"log2"} (values already on the model scale).}

\item{reference_value}{Positive reference used for natural-scale data:
\code{log2(value / reference_value)}.}

\item{lower_limit, upper_limit}{Optional censoring limits. Each may be a
scalar, a numeric vector with one value per row, a named numeric vector by
biomarker, or the name of a numeric column. Use \code{NA} for no limit on a row.}

\item{censoring}{Optional censoring indicator or column name. Accepted values
are \code{"none"}, \code{"left"}, and \code{"right"} (or \code{0}, \code{-1}, and \code{1}). When
omitted, measurements at or beyond supplied limits are classified
automatically.}

\item{priors}{An \code{\link[=epikinetics_priors]{epikinetics_priors()}} object.}
}
\value{
An \code{epikinetics_data} object. Its public components include
\code{input_data}, \code{observations}, \code{participants}, \code{model_frame},
\code{model_matrix}, \code{mappings}, and \code{stan_data}. \code{mappings$design_columns}
links numeric design columns back to terms and treatment levels. Use
\code{\link[=stan_data]{stan_data()}} and \code{model.matrix()} as accessors.
}
\description{
Converts an ordinary data frame into the complete, participant-indexed
representation used by the Stan model. This is a separate public operation
from \code{\link[=fit_epikinetics]{fit_epikinetics()}} so that transformations, censoring, indices,
mappings, and the design matrix can be inspected before compilation or
sampling.
}
\details{
The model assumes one focal exposure per participant. Covariates in
\code{formula} must likewise be constant within participant. Standard R contrast
handling is used: include the formula intercept (for example \code{~ age + sex}),
and epikinetics removes that intercept because biomarker-specific population
parameters already provide it.
}
