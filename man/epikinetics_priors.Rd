% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R, R/priors.R
\name{plot.epikinetics_priors}
\alias{plot.epikinetics_priors}
\alias{epikinetics_priors}
\title{Plot prior kinetic trajectories}
\usage{
\method{plot}{epikinetics_priors}(
  x,
  ...,
  times = 0:150,
  ndraws = 1000,
  reference_value = 1,
  scale = c("model", "response")
)

epikinetics_priors(
  baseline = c(mean = 6, sd = 2),
  time_to_peak = c(mean = 10, sd = 5),
  waning_duration = c(mean = 50, sd = 20),
  boost_rate = c(mean = 0.25, sd = 0.1),
  early_waning_rate = c(mean = 0.02, sd = 0.01),
  late_waning_rate = c(mean = 0.002, sd = 0.005),
  participant_sd = c(baseline = 0.75, time_to_peak = 0.35, waning_duration = 0.5,
    boost_rate = 0.35, early_waning_rate = 0.5, late_waning_rate = 0.75),
  covariate_sd = c(baseline = 0.5, time_to_peak = 0.35, waning_duration = 0.5, boost_rate
    = 0.35, early_waning_rate = 0.5, late_waning_rate = 0.75),
  observation_sd = 1
)
}
\arguments{
\item{x}{An \code{epikinetics_priors} object.}

\item{...}{Reserved for compatibility with the \code{\link[=plot]{plot()}} generic.}

\item{times}{Non-negative prediction times.}

\item{ndraws}{Number of prior trajectories to simulate.}

\item{reference_value}{Positive reference value used to convert model-scale
values to the response scale when \code{scale = "response"}.}

\item{scale}{Output scale: \code{"model"} for log2-relative values or
\code{"response"} for natural measurement values.}

\item{baseline, time_to_peak, waning_duration, boost_rate, early_waning_rate, late_waning_rate}{Numeric length-two vectors giving the population prior mean and standard
deviation.}

\item{participant_sd}{Named positive vector giving half-Normal scales for
participant-level standard deviations.}

\item{covariate_sd}{Named positive vector giving Normal standard deviations
for regression coefficients.}

\item{observation_sd}{Positive half-Normal scale for observation error.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.

An object of class \code{epikinetics_priors}.
}
\description{
Draw population-level kinetic curves from the configured priors. The band
shows the pointwise 95\% prior interval and the line shows the pointwise
prior median.

Construct and inspect the priors used by \code{\link[=fit_epikinetics]{fit_epikinetics()}}. Population
priors are Normal distributions parameterised by \code{c(mean, sd)}. Positive
kinetic quantities use Normal priors truncated at zero. Their prior means
must be non-negative so that the inverse-CDF parameterisation remains
numerically stable.
}
\details{
Baseline is expressed on the model's log2 scale. Time parameters are in the
same units as the input time columns (normally days). Rates are log2 units
per time unit. Participant standard deviations act additively for baseline
and on the log scale for positive time/rate parameters. Covariate priors use
the same convention.
}
\examples{
priors <- epikinetics_priors(
  time_to_peak = c(mean = 12, sd = 4),
  early_waning_rate = c(mean = 0.025, sd = 0.01)
)
priors
}
