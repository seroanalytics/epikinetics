% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_epikinetics}
\alias{fit_epikinetics}
\title{Fit a hierarchical biomarker kinetics model}
\usage{
fit_epikinetics(
  model_data,
  chains = 4,
  parallel_chains = chains,
  threads_per_chain = 1,
  grainsize = NULL,
  adapt_delta = 0.9,
  max_treedepth = 12,
  ...
)
}
\arguments{
\item{model_data}{An object returned by \code{\link[=prepare_epikinetics_data]{prepare_epikinetics_data()}}.}

\item{chains}{Number of Markov chains.}

\item{parallel_chains}{Number of chains to run concurrently.}

\item{threads_per_chain}{Number of Stan threads used by each chain. The model
is always compiled with threading enabled.}

\item{grainsize}{Number of participants handled by each \code{reduce_sum} task.
By default it is chosen from the participant and thread counts.}

\item{adapt_delta}{Target acceptance probability. The conservative default
is appropriate for this nonlinear hierarchy and may be overridden after
inspecting diagnostics.}

\item{max_treedepth}{Maximum NUTS tree depth.}

\item{...}{Additional arguments passed to the CmdStanR model's \verb{$sample()}
method, such as \code{iter_warmup}, \code{iter_sampling}, and \code{seed}.}
}
\value{
An object of class \code{epikinetics_fit}.
}
\description{
Fits the epikinetics piecewise-linear model with CmdStanR and returns a
self-contained S3 fit object. The likelihood supports uncensored, left-
censored, and right-censored observations. Parallel chains and Stan threads
within each chain can be used together. Data validation and transformation
are deliberately performed beforehand by \code{\link[=prepare_epikinetics_data]{prepare_epikinetics_data()}}.
}
\examples{
\dontrun{
dat <- utils::read.csv(
  system.file("extdata", "delta.csv", package = "epikinetics")
)
model_data <- prepare_epikinetics_data(
  dat,
  formula = ~ infection_history,
  lower_limit = 5,
  upper_limit = 2560
)
model_data
model.matrix(model_data)
stan_data(model_data)

fit <- fit_epikinetics(
  model_data,
  chains = 4,
  parallel_chains = 4,
  threads_per_chain = 2
)
}
}
