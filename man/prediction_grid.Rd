% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{prediction_grid}
\alias{prediction_grid}
\title{Construct default covariate profiles for prediction}
\usage{
prediction_grid(
  x,
  categorical = c("observed", "cartesian"),
  continuous = c("median", "mean")
)
}
\arguments{
\item{x}{An \code{epikinetics_data} or \code{epikinetics_fit} object.}

\item{categorical}{Use combinations \code{"observed"} in the participant data or
the full \code{"cartesian"} product of fitted categorical levels.}

\item{continuous}{Hold continuous covariates at their participant-level
\code{"median"} (default) or \code{"mean"}.}
}
\value{
An ordinary data frame with one row per prediction profile. The
\code{.profile} column is a stable row identifier; remaining columns are the
original variables used by the model formula.
}
\description{
Builds inspectable participant-level profiles from the formula and model
frame stored by \code{\link[=prepare_epikinetics_data]{prepare_epikinetics_data()}}. By default, categorical
profiles are the combinations observed among fitted participants; this
avoids silently extrapolating to unsupported combinations. Continuous
covariates are held at their participant-level median. Use
\code{categorical = "cartesian"} to request every combination of fitted factor
levels, or pass an explicit \code{newdata} data frame to \code{\link[=predict]{predict()}}.
}
\details{
These are conditional profiles, not averages over the fitted covariate
distribution. Interactions and transformed terms are subsequently evaluated
with the original terms object and contrasts when \code{\link[=predict]{predict()}} constructs the
numeric design matrix.
}
\examples{
dat <- utils::read.csv(
  system.file("extdata", "delta.csv", package = "epikinetics")
)
prepared <- prepare_epikinetics_data(
  dat,
  formula = ~ infection_history,
  lower_limit = 5,
  upper_limit = 2560
)
prediction_grid(prepared)
}
