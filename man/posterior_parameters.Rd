% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior.R
\name{posterior_parameters}
\alias{posterior_parameters}
\title{Extract labelled kinetic parameters}
\usage{
posterior_parameters(
  x,
  level = c("population", "profile", "participant", "regression"),
  summary = TRUE,
  newdata = NULL,
  participants = NULL,
  ndraws = NULL,
  probs = c(0.025, 0.975),
  derived = TRUE
)
}
\arguments{
\item{x}{An \code{epikinetics_fit} object.}

\item{level}{One of \code{"population"}, \code{"profile"}, \code{"participant"}, or
\code{"regression"}. Population output is the biomarker-specific model
intercept. Profile output applies the fitted covariate effects to the
conditional profiles from \code{\link[=prediction_grid]{prediction_grid()}} or \code{newdata}, but excludes
participant random effects.}

\item{summary}{If \code{TRUE}, return means, medians, SDs, and intervals. If
\code{FALSE}, return one labelled row per posterior draw.}

\item{newdata}{Optional participant-level covariate profiles when
\code{level = "profile"}. \code{NULL} uses \code{\link[=prediction_grid]{prediction_grid()}}.}

\item{participants}{Optional participant ids when \code{level = "participant"}.}

\item{ndraws}{Optional maximum number of posterior draws to use.}

\item{probs}{Lower and upper interval probabilities.}

\item{derived}{Include peak values, transition values, and waning half-lives.}
}
\value{
A tidy data frame. Summaries use one row per parameter and group;
unsummarised results retain parameter columns and \code{.draw}. Regression
output includes the original formula term, design-column name, and decoded
treatment level/reference when the contrast has that interpretation.
}
\description{
Extract labelled kinetic parameters
}
