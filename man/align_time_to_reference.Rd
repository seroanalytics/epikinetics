% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time.R
\name{align_time_to_reference}
\alias{align_time_to_reference}
\title{Align longitudinal measurements to a reference date}
\usage{
align_time_to_reference(
  data,
  measurement_date,
  reference,
  id = NULL,
  time = "time_since_reference",
  missing = c("error", "keep")
)
}
\arguments{
\item{data}{A data frame containing longitudinal observations.}

\item{measurement_date}{Name of the \code{Date} column containing measurement or
sample-collection dates.}

\item{reference}{Either the name of a \code{Date} column in \code{data}, normally
repeated within participant, or one shared \code{Date} value.}

\item{id}{Optional participant-id column. When \code{reference} names a column,
supplying \code{id} checks that its non-missing values agree within each
participant.}

\item{time}{Name of the numeric output column to add.}

\item{missing}{How missing measurement or reference dates are handled.
\code{"error"} (the default) reports them; \code{"keep"} retains the rows and writes
\code{NA_real_} to \code{time}.}
}
\value{
\code{data} with its original columns unchanged and an additional numeric
time column measured in days relative to \code{reference}.
}
\description{
Convert calendar-date observations into numeric days relative to either a
participant-specific reference column (for example, each participant's
vaccination date) or one shared reference date. The original date columns
are retained and a new numeric column is added.
}
\details{
\code{measurement_date} and a reference column must contain base R \link{Date}
values. \code{POSIXct} is deliberately not converted implicitly because a hidden
timezone or time-of-day conversion can shift calendar dates; convert it
explicitly with \code{\link[=as.Date]{as.Date()}} first. Negative values are retained, making
pre-reference observations visible during data checking. The current
single-exposure model itself accepts observations on or after time zero.
}
\examples{
observations <- data.frame(
  participant = c("A", "A", "B", "B"),
  exposure_date = as.Date(c(
    "2024-01-10", "2024-01-10", "2024-02-01", "2024-02-01"
  )),
  sample_date = as.Date(c(
    "2024-01-08", "2024-01-17", "2024-01-30", "2024-02-15"
  ))
)

align_time_to_reference(
  observations,
  measurement_date = "sample_date",
  reference = "exposure_date",
  id = "participant",
  time = "time_since_exposure"
)

align_time_to_reference(
  observations,
  measurement_date = "sample_date",
  reference = as.Date("2024-01-01"),
  time = "time_since_study_start"
)
}
